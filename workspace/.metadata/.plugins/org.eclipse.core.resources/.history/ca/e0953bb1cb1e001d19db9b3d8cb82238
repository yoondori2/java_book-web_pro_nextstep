package tdd;

public class AccountTest {
	
	public void testAccount(){
		Account account = new Account();
		//계좌가 정상적으로 생성됏는지 확인하는 부분을 넣자. 
		if(account == null) {
			throw new Exception("계좌생성 실패");
		}
	}
}
